import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryLight = Color(0xFFEBD8F5);
  static const Color primary = Color(0xFFFFE4EF);
  static const Color accent = Color(0xFFCCDDFF);
  static const Color white = Color(0xFFFFFFFF);
  
  static const Color textPrimary = Color(0xFF333333); // Dark grey for text
  static const Color textSecondary = Color(0xFF666666); // Light grey for secondary text
}
